---
name: Jupyter Notebook
category: Interactive Apps
subcategory: Servers
role: batch_connect
description: |
  This app will launch a Jupyter Notebook server on one or more nodes. By default, this app uses the anaconda/python_3.6 module. 
  If you need to use a different python install, then use the custom environment below. Be sure to unload the anaconda/python_3.6 module.
